<assembly xmlns="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-assembly-plugin/assembly/1.1.0 http://maven.apache.org/xsd/assembly-1.1.0.xsd">
  <!-- the id is used as artifact's classifier when deploying. -->
  <id>interface</id>
  <formats>
    <format>zip</format>
  </formats>
  <baseDirectory>/</baseDirectory>
  <fileSets>
    <fileSet>
      <directory>${project.artifactId}/include</directory>
      <outputDirectory>${project.artifactId}/include</outputDirectory>
      <includes>
        <!-- Todo : add specific include here if needed -->
        <include>**/**</include>
      </includes>
      <excludes>
        <!-- Todo : add specific exludes here if needed -->
      </excludes>
    </fileSet>
    <fileSet>
      <directory>../sample/sample/include</directory>
      <outputDirectory>sample/include</outputDirectory>
      <includes>
        <!-- Todo : add specific include here if needed -->
        <include>**/**</include>
      </includes>
      <excludes>
        <!-- Todo : add specific exludes here if needed -->
      </excludes>
    </fileSet>
    <fileSet>
      <directory>../modulea/modulea/include</directory>
      <outputDirectory>modulea/include</outputDirectory>
      <includes>
        <!-- Todo : add specific include here if needed -->
        <include>**/**</include>
      </includes>
      <excludes>
        <!-- Todo : add specific exludes here if needed -->
      </excludes>
    </fileSet>
    <fileSet>
      <directory>../moduleb/moduleb/include</directory>
      <outputDirectory>moduleb/include</outputDirectory>
      <includes>
        <!-- Todo : add specific include here if needed -->
        <include>**/**</include>
      </includes>
      <excludes>
        <!-- Todo : add specific exludes here if needed -->
      </excludes>
    </fileSet>
  </fileSets>
</assembly>
